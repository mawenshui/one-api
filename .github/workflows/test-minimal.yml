name: test-minimal

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Greeting
        run: echo "Hello from $(uname -a)"
      - name: Disk free
        run: df -h /
      - name: Who's running
        run: id
